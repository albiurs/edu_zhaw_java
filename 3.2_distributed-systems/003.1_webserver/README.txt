Instruktionen
-------------

1. Webserver starten:

mit dem Skript mywebserver bzw. ./mywebserver.sh oder explizit mit java -cp bin MyWebserver
	oder
java -Dcontent.types.user.table=content-types.properties -cp bin MyWebserver

2. Aufruf im Browser: 

http://localhost:50000/
